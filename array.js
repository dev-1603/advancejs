
//  initialize array of fixed length  and fill it with some value 
// `n` is the size you want to initialize your array
// `0` is what the array will be filled with (can be any other value)   bgut it will fail in typescript
Array(n).fill(0)
